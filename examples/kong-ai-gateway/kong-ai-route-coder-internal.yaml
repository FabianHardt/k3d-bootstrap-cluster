apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ai-proxy-coder-internal
  namespace: kong
  annotations:
    konghq.com/plugins: ai-proxy-coder,ai-key-auth
    konghq.com/strip-path: "true"
spec:
  parentRefs:
    - name: kong
      namespace: kong
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /coder
      backendRefs:
        - name: ollama
          namespace: ai-platform
          port: 11434
